$('#card-list').html("<%= j (render 'credit_cards/credit_card_list') %>");
$("#flash-message").html("<%= j (render 'layouts/flash_message') %>");


// close modal
$('#newCardModal').modal('hide');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();